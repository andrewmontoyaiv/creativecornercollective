<div class="bg-white rounded shadow p-4 text-center">
    <img src="{{ artist.photo }}" alt="{{ artist.name }}" class="rounded-full h-24 w-24 mx-auto mb-2" />
    <p class="font-bold">{{ artist.name }}</p>
    <p class="text-sm text-gray-600">{{ artist.bio }}</p>
    <a href="{{ artist.link }}" target="_blank" class="text-blue-600 hover:underline">Follow Their Work</a>
  </div>