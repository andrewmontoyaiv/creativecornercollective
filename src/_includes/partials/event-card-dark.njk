<div class="bg-[#3D0050] rounded shadow p-4 text-center">
  <img src="{{ event.image }}" alt="{{ event.alt }}" class="rounded mb-2" />
  <p class="font-semibold text-white">{{ event.title }}</p>
  <p class="text-sm text-purple-300">{{ event.date }}</p>
</div>