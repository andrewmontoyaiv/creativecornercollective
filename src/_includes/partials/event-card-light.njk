<div class="bg-white rounded shadow p-4 text-center">
  <img src="{{ event.image }}" alt="{{ event.alt }}" class="rounded mb-2" />
  <p class="font-semibold">{{ event.title }}</p>
  <p class="text-sm text-gray-500">{{ event.date }}</p>
</div>