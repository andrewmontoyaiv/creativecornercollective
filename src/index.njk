---
title: "Creative Corner Collective"
layout: base-light
---


<main class="relative scroll-smooth">
  {% set theme = "main" %}    
  {% include "partials/header.njk" %}
  <section class="px-4 sm:px-10 max-w-6xl mx-auto w-full text-center mt-10 flex flex-col items-center">
    {% svg "cc-logo" %}

    <div class="w-96 mb-5">
      {% include "partials/toggle-switch.njk" %}
    </div>
  
    <div class="relative w-full">
      <!-- Background SVG -->
      <div class="absolute inset-0 -z-10 pointer-events-none">
        {% svg "paper-cutout", "w-full h-full object-fill fill-gray-200" %}
      </div>
  
      <!-- Foreground content -->
      <div class="p-6 sm:p-12 pt-16 sm:pt-24 flex flex-col items-center relative z-10">
        <div class="w-full max-w-6xl px-4 flex flex-col items-center">
          <div class="w-full md:w-9/12">
            <h2 class="text-2xl font-caveat font-bold mb-4 text-center sm:text-left">Next Event</h2>
      
            <div class="flex flex-col sm:flex-row rounded-xl overflow-hidden shadow-lg">
              <!-- Image and overlay text -->
              <div class="relative w-full sm:w-2/3 h-64 sm:h-auto">
                <img src="/assets/next-event.jpg" alt="Creative Corner Sessions" class="w-full h-full object-cover" />
              </div>
        
              <!-- Event info + actions -->
              <div class="bg-[#f7f5ee] w-full sm:w-1/3 p-6 flex flex-col justify-center items-center text-center space-y-4">
                <p class="text-xl font-mono tracking-wide">May 30th, 2025</p>
                <a href="/get-tickets/" class="bg-blue-400 text-white text-lg font-semibold px-6 py-3 rounded-xl shadow-md hover:bg-blue-500 transition">
                  Get Tickets
                </a>
                <!--
                  <a href="#" class="text-blue-400 hover:underline text-sm tracking-wide">
                    Find Out More
                  </a>
                -->

              </div>
            </div>
          </div>

          <!-- Countdown timer -->
          <div 
            class="text-center mt-10 mb-10"
            x-data="() => countdownTimer('2025-05-30T00:00:00')"
            x-init="start()"
          >
            <h2 class="text-xl font-caveat mb-6">Vibes return in...</h2>
      
            <div class="flex justify-center flex-wrap gap-4 sm:gap-10 text-3xl font-bold font-[DM Sans]">
              <div class="flex flex-col items-center">
                <span x-text="String(time.days).padStart(2, '0')"></span>
                <span class="text-sm font-normal tracking-wider uppercase">Days</span>
              </div>
              <div class="text-2xl mt-1 hidden sm:block">:</div>
              <div class="flex flex-col items-center">
                <span x-text="String(time.hours).padStart(2, '0')"></span>
                <span class="text-sm font-normal tracking-wider uppercase">Hours</span>
              </div>
              <div class="text-2xl mt-1 hidden sm:block">:</div>
              <div class="flex flex-col items-center">
                <span x-text="String(time.minutes).padStart(2, '0')"></span>
                <span class="text-sm font-normal tracking-wider uppercase">Minutes</span>
              </div>
              <div class="text-2xl mt-1 hidden sm:block">:</div>
              <div class="flex flex-col items-center">
                <span x-text="String(time.seconds).padStart(2, '0')"></span>
                <span class="text-sm font-normal tracking-wider uppercase">Seconds</span>
              </div>
            </div>
          </div>
        </div>
      </div>



    </div>
  </section>
  
  <section class="px-4 sm:px-10 max-w-6xl mx-auto text-center mt-12">
    <h3 class="text-2xl font-bold mb-4">Where Creativity Finds Its Voice</h3>
    <p class="max-w-xl mx-auto">Creative Corner Collective is a vibrant community space where artists, musicians, and visionaries come together to share music and good vibes. Whether you’re here to celebrate self-expression, foster meaningful connection, or ignite collaborative creativity — welcome.</p>
  </section>

  <section id="socials" class="px-4 sm:px-10 max-w-6xl mx-auto text-center mt-12">
  
    <div class="flex justify-center gap-8 text-3xl">
      <a href="https://instagram.com/yourhandle" target="_blank" class="transition-transform hover:scale-110 text-pink-500 hover:text-pink-600">
        <i class="fab fa-instagram"></i>
      </a>
      <a href="https://tiktok.com/@yourhandle" target="_blank" class="transition-transform hover:scale-110 text-black hover:text-gray-800">
        <i class="fab fa-tiktok"></i>
      </a>
      <a href="https://facebook.com/yourhandle" target="_blank" class="transition-transform hover:scale-110 text-blue-600 hover:text-blue-700">
        <i class="fab fa-facebook-f"></i>
      </a>
      <a href="https://youtube.com/@yourhandle" target="_blank" class="transition-transform hover:scale-110 text-red-600 hover:text-red-700">
        <i class="fab fa-youtube"></i>
      </a>
    </div>
  </section>

  <div class="w-full p-2 text-gray-400">
    {% include "partials/separator.njk" %}
  </div>
  
  <section id="events" class="px-4 sm:px-10 max-w-6xl mx-auto mt-12">
    <h3 class="text-xl font-bold text-center mb-6">Our Past Events</h3>
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
      {% for event in events %}
        {% include "partials/event-card-light.njk" %}
      {% endfor %}
    </div>
    <div class="text-center mt-4">
      {# <a href="/events" class="text-blue-500 hover:underline">Show All Events</a> #}
    </div>
  </section>

  <div class="w-full p-2 text-gray-400">
    {% include "partials/separator.njk" %}
  </div>
  
  <section id="artists" class="px-4 sm:px-10 max-w-6xl mx-auto mt-12">
    <h3 class="text-xl font-bold text-center mb-6">Artist Spotlight</h3>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
      {% for artist in artists %}
        {% include "partials/artist-card-light.njk" %}
      {% endfor %}
    </div>
  </section>
  
  <div class="w-full relative overflow-hidden">
    <div style="margin: 0 -100px;" class="absolute inset-y-0 inset-x-[-100px] -z-10 pointer-events-none">
      {% svg "paper-cutout", "w-full h-full object-fill fill-gray-200" %}
    </div>
    {% include "partials/artist-signup.njk" %}
  </div>

  
  {% include "partials/newsletter-signup.njk" %}


  <div class="w-full p-2 text-gray-400">
    {% include "partials/separator.njk" %}
  </div>
  
  <section class="px-4 sm:px-10 max-w-6xl mx-auto mt-12 text-center">
    <h4 class="text-md font-bold mb-2">Supporters</h4>
    <img src="/assets/diamond-dogs-logo.png" alt="Diamond Dogs Logo" class="mx-auto h-16" />
  </section>
</main>

